import Link from 'next/link';
import {publicTierFor,tiersFor,type MembershipKind} from '@/lib/memberships';
export function MembershipPlans({kind}:{kind:MembershipKind}){return <section className="membership-plans" aria-label={`${kind} memberships`}>{tiersFor(kind).map((tier,index)=>{const publicTier=publicTierFor(tier.code)!;return <article className={`membership-card tier-${tier.code}`} key={tier.code}><div className="membership-card-top"><div className="tier-swatch" aria-hidden="true"><span>R</span></div><span className="edition-number">FOUNDING EDITION · {String(index+1).padStart(2,'0')}</span></div><div><span className="eyebrow">{kind} membership</span><h2>{tier.name}</h2><strong>{tier.price===0?'FREE':`US$${tier.price}`} <small>one-time</small></strong><p>{tier.summary}</p><blockquote>{tier.legacy}</blockquote></div><div className="membership-includes"><span>Recognition & privileges</span><ul>{tier.benefits.map(item=><li key={item}>{item}</li>)}</ul></div>{tier.launchBenefits&&<div className="launch-note"><b>Coming with launch</b>{tier.launchBenefits.map(item=><p key={item}>{item}</p>)}</div>}<Link className="button dark" href={`/auth/sign-up?type=${kind}&tier=${publicTier.publicTier}`}>Choose {tier.name}</Link></article>})}</section>}
