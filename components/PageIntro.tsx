import Image from 'next/image';export function PageIntro({eyebrow,title,copy,image=true}:{eyebrow:string;title:string;copy:string;image?:boolean}){return <section className="page-intro"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{copy}</p></div>{image&&<Image src="/images/founding-community.png" width={1536} height={1024} sizes="(max-width: 800px) 100vw, 48vw" alt="Travelers and hospitality hosts sharing stories in a sunlit courtyard" priority/>}</section>}
