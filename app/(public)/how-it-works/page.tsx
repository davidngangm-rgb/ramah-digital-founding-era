import type {Metadata} from 'next';import {PublicShell} from '@/components/PublicShell';import {PageIntro} from '@/components/PageIntro';export const metadata:Metadata={title:'How Founding Membership Works',description:'The clear path from Ramah account creation to verified founder identity.',alternates:{canonical:'/how-it-works'}};export default function Page(){const steps=['Create one Ramah account','Verify your email address','Choose traveler or host','Submit the saved application','Ramah reviews the submitted information','Receive Founder approval','Receive activation and founder number','Open the founder portal and Ramah App'];return <PublicShell><PageIntro eyebrow="How it works" title="A careful path to a permanent record." copy="Every founder identity is verified by the backend. No number, badge or credential is created by the browser." image={false}/><section className="process">{steps.map((s,i)=><article key={s}><span>{String(i+1).padStart(2,'0')}</span><h2>{s}</h2></article>)}</section></PublicShell>}
