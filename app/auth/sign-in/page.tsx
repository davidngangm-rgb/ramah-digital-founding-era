import type {Metadata} from 'next';import Link from 'next/link';import {AuthForm} from '@/components/AuthForm';export const metadata:Metadata={title:'Sign in',robots:{index:false,follow:false}};export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const q=await searchParams;return <main id="main" className="auth-page"><Link className="brand auth-brand" href="/"><span>R</span><b>RAMAH</b></Link><section><div><span className="eyebrow">Founder portal</span><h1>Return to your place in the story.</h1><p>Use the same email and password as the Ramah App.</p></div><AuthForm mode="in" error={q.error} next={q.next}/><p className="auth-switch">New to Ramah? <Link href="/auth/sign-up">Create your account</Link></p></section></main>}
