import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {PublicShell} from '@/components/PublicShell';import {getFounder} from '@/lib/supabase/queries';export const metadata:Metadata={title:'Verify Founder',robots:{index:false,follow:false}};export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,f=await getFounder(slug);if(!f)notFound();return <PublicShell><section className="verification"><span className="seal">R</span><span className="eyebrow">Public verification</span><h1>Founder identity confirmed.</h1><strong>{f.founder_number}</strong><p>{f.display_name} is recorded as a Ramah {f.founder_type==='host'?'Founding Host':'Founding Traveler'}.</p><small>This public result confirms founder identity only. It does not disclose current payment, private credential or benefit information.</small></section></PublicShell>}
