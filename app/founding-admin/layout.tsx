import type {Metadata} from 'next';import {redirect} from 'next/navigation';import {createClient} from '@/lib/supabase/server';import {getStaffAccess,hasFoundingAdminAccess} from '@/lib/staff';import {PortalHeader} from '@/components/PortalHeader';export const metadata:Metadata={title:'Founding Admin',robots:{index:false,follow:false}};export default async function Layout({children}:{children:React.ReactNode}){const sb=await createClient();const access=await getStaffAccess(sb);if(!hasFoundingAdminAccess(access))redirect('/portal');return <><PortalHeader admin displayName={access?.displayName}/><main id="main" className="admin-shell">{children}</main></>}
