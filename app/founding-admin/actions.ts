"use server";import {createClient} from '@/lib/supabase/server';import {redirect} from 'next/navigation';import {revalidatePath} from 'next/cache';const t=(f:FormData,k:string)=>String(f.get(k)??'').trim();
export async function reviewApplication(form:FormData){const sb=await createClient();const {error}=await sb.rpc('review_founding_application',{p_application_id:t(form,'id'),p_decision:t(form,'decision'),p_reason:t(form,'reason'),p_request_id:crypto.randomUUID(),p_grace_eligible:false});if(error)redirect(`/founding-admin?error=${encodeURIComponent(error.message)}`);revalidatePath('/founding-admin')}
export async function activateFounder(form:FormData){const sb=await createClient();const {error}=await sb.rpc('activate_founding_member',{p_application_id:t(form,'id'),p_request_id:t(form,'requestId'),p_reason:t(form,'reason')});if(error)redirect(`/founding-admin?error=${encodeURIComponent(error.message)}`);revalidatePath('/founding-admin')}
export async function reviewPublication(form:FormData){const sb=await createClient();const {error}=await sb.rpc('review_founder_publication',{p_user_id:t(form,'id'),p_approve:t(form,'decision')==='approve',p_reason:t(form,'reason'),p_request_id:crypto.randomUUID()});if(error)redirect(`/founding-admin?error=${encodeURIComponent(error.message)}`);revalidatePath('/founding-admin')}
export async function markLaunchReady(form:FormData){const sb=await createClient();const {error}=await sb.rpc('mark_founding_inventory_launch_ready',{p_property_id:t(form,'id'),p_reason:t(form,'reason'),p_request_id:crypto.randomUUID()});if(error)redirect(`/founding-admin?error=${encodeURIComponent(error.message)}`);revalidatePath('/founding-admin')}
export async function activateLaunchInventory(form:FormData){const sb=await createClient();const {error}=await sb.rpc('activate_founding_launch_inventory',{p_property_id:t(form,'id'),p_reason:t(form,'reason'),p_request_id:crypto.randomUUID()});if(error)redirect(`/founding-admin?error=${encodeURIComponent(error.message)}`);revalidatePath('/founding-admin')}
export async function activateGlobalLaunch(form:FormData){const sb=await createClient();const {error}=await sb.rpc('activate_all_founding_launch_inventory',{p_reason:t(form,'reason'),p_request_id:crypto.randomUUID()});if(error)redirect(`/founding-admin?error=${encodeURIComponent(error.message)}`);revalidatePath('/founding-admin')}
export async function setFoundingTestAccess(form:FormData){const sb=await createClient();const enabled=t(form,'enabled')==='true';const {error}=await sb.rpc('set_founding_test_access',{p_email:t(form,'email'),p_enabled:enabled,p_reason:t(form,'reason'),p_request_id:crypto.randomUUID()});if(error)redirect(`/founding-admin/testing?error=${encodeURIComponent(error.message)}`);redirect(`/founding-admin/testing?notice=${encodeURIComponent(enabled?'Testing access granted. The user must sign out and sign in again to refresh the secure claim.':'Testing access revoked.')}`)}
